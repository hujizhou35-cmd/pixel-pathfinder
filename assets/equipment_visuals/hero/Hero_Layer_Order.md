# Hero Layer Order

1. `background_element_fx` — scale=1, rotation=0°
2. `accessory_back` — scale=1, rotation=0°
3. `weapon_back` — scale=1, rotation=0°
4. `neutral_body` — scale=1, rotation=0°
5. `pants` — scale=1, rotation=0°
6. `boots` — scale=1, rotation=0°
7. `armor` — scale=1, rotation=0°
8. `face_head_hair` — scale=1, rotation=0°
9. `helmet` — scale=1, rotation=0°
10. `back_arm` — scale=1, rotation=0°
11. `front_arm` — scale=1, rotation=0°
12. `weapon_front` — scale=1, rotation=0°
13. `accessory_front` — scale=1, rotation=0°
14. `foreground_element_fx` — scale=1, rotation=0°
